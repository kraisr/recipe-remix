import React from 'react';
import SearchBar from '../../components/Searchbar/Searchbar.js'; // Import the SearchBar component

function Home() {
    return (
        <div>
            <SearchBar />
            {/* ... (any other UI elements you might add to Home in the future) */}
        </div>
    );
}

export default Home;

